#!/bin/bash

mydir=$(dirname "$(readlink -f "$0")")
_radio=$mydir/_radio
xx=$mydir/libexec/»»

if "$_radio" --action-given "$@"; then
    exec "$_radio" "$@"
else
    rlwrap --command-name radio "$xx" "$_radio" "$@"
fi
